class Arrays {
    static function main() {
        var a = [1, 2, 3];
        trace(a);
        var b = [];
        trace(b);
        var arr = [1, 2, 3];
        arr.push(4);
        arr.pop();
        var first = arr[0];
        trace(arr);
        trace(first);
    }
}